# Knowledge Routing RAG v0 实验方案

## 1. 实验目标

验证以下核心假设：

> 在相同文档、相同 embedding、相同生成模型、相近最终上下文预算下，基于受约束知识路由的多跳检索，可以比纯 Vector RAG 获得更高的回答准确率，同时降低高扇出节点造成的上下文污染，并把延迟控制在可接受范围。

本实验不以“证明 GraphRAG 有效”为目标。

真正需要验证的是：

**Routing 是否能在不扩大最终上下文的情况下，比平面检索找到更完整、更干净的证据链。**

---

# 2. 核心研究问题

实验回答五个问题。

### RQ1：准确率

Routing-RAG 是否高于重新测得的纯 RAG baseline？

历史参考：

```text
Vector-only ≈ 40%
Legacy Vector+FTS+Graph ≈ 35%
```

但本实验重新测量。

### RQ2：规模增长

当知识库由：

```text
20 docs
→ 50 docs
→ 100 docs
```

增长时，Routing-RAG 是否比纯 RAG 更抗干扰？

尤其观察答案证据仍位于最初 20 篇文档中，但增加 80 篇干扰文档之后的表现。

### RQ3：Hub contamination

当正确路径经过或靠近：

```text
degree = 5
10
20
50+
```

的公共知识节点时，Routing-RAG 是否能阻止邻居爆炸？

### RQ4：多跳

对于：

```text
A → B
A → B → C
A → B → C → D
```

Routing 是否随着 hop 增加表现出比纯 RAG 更小的准确率衰减？

### RQ5：代价

准确率提升是否需要付出不可接受的：

```text
latency
tokens
LLM calls
retrieval candidates
```

---

# 3. 语料设计：D20 ⊂ D50 ⊂ D100

这是整个实验最重要的控制变量之一。

不要随机制作三套知识库。

必须：

```text
D20 ⊂ D50 ⊂ D100
```

例如：

```text
D20
doc001 ... doc020

D50
doc001 ... doc050

D100
doc001 ... doc100
```

这样才可以观察：

> 当答案完全没有变化，只增加 distractor 文档时，系统会不会退化？

三个 corpus 使用完全相同的预处理格式。

优先直接使用已经完成预处理的：

```text
.md
.txt
```

不要在本实验中同时重新研究 PDF parsing、OCR、chunking 等问题。

---

# 4. Benchmark 设计

建议总共建立 **120 道题**。

## Core-60

60 道题的全部 gold evidence 必须位于 D20 中。

因此这 60 道题分别在：

```text
D20
D50
D100
```

重复测试。

这是整个实验最重要的数据。

它测试的是：

```text
答案没变
+
干扰文档越来越多
→ 系统发生什么？
```

## Extension-50

30 道题至少需要 doc021–doc050 中的证据。

测试：

```text
D50
D100
```

## Extension-100

30 道题至少需要 doc051–doc100 中的证据。

只测试：

```text
D100
```

---

# 5. 问题类型

不需要人为平均分布，但 benchmark 中至少保证：

```text
single-hop            ≥ 15%
2-hop                 ≥ 25%
3-hop+                ≥ 20%
hub/high-fanout       ≥ 20%
version/temporal      ≥ 10%
exception/conflict    ≥ 10%
```

标签允许重叠。

例如一道题可以同时是：

```text
3-hop
hub
temporal
```

重点不是制造复杂题，而是覆盖真实失败模式。

---

# 6. 每道题必须有 Gold Evidence

benchmark 不只保存：

```text
question
answer
```

而要保存：

```json
{
  "qid": "Q0042",
  "question": "...",
  "gold_answer": "...",
  "gold_documents": ["doc007", "doc013"],
  "gold_spans": ["..."],
  "gold_path": [
    ["node_A", "REFERENCES", "node_B"],
    ["node_B", "SUPERSEDES", "node_C"]
  ],
  "hop_count": 2,
  "tags": ["multi_hop", "temporal", "hub"],
  "answerable_in": ["D20", "D50", "D100"]
}
```

这是必须做的。

否则最终只知道：

> 模型答错了。

却不知道是：

```text
retrieval 错
routing 错
evidence filtering 错
generator 错
```

---

# 7. Dev / Test 隔离

120 道题建立完成之后立即固定：

```text
Dev  = 24
Test = 96
```

按题型分层随机划分。

所有：

```text
top-k
hop budget
hub threshold
relation weight
reranker threshold
prompt
```

只能看 Dev 调。

一旦开始正式 Test：

**禁止继续根据 Test 结果调整参数。**

建议在仓库中提交：

```text
benchmark/split.json
```

并固定 commit。

---

# 8. 实验系统

至少运行以下六组。

| ID | 系统 | 作用 |
|---|---|---|
| B0 | Vector RAG | 主 baseline |
| B1 | Vector + FTS | 强平面检索 baseline |
| B2 | Legacy Graph | 如果旧代码还在，用来复现 35% 退化 |
| K1 | RIFT-style Routing | 测试聚合与 Hub 隔离 |
| K2 | K1 + EIGRP-style Feasibility | 测试下一跳约束 |
| K3 | K2 + SRv6-style Segment Plan | 测试显式多跳路径程序 |
| K4 | K3 + Evidence Gap fallback | 完整 Routing-RAG |

B2 如果无法可靠复现，可以标记为 optional。

真正必须的是：

```text
B0
B1
K1
K2
K3
K4
```

---

# 9. 所有系统共享相同基础条件

固定：

```text
corpus
chunking
embedding model
vector database
FTS implementation
generator model
temperature
system prompt
answer prompt
hardware
```

最终交给 Generator 的 evidence context 设置统一上限。

第一轮建议：

```text
max_evidence_tokens = 4000
```

不要允许 Routing-RAG 因为塞了更多 token 获胜。

记录控制器本身额外消耗的 token，但它不计入最终 evidence budget。

---

# 10. Knowledge LSDB

采用 LSVR 启发的设计：

**知识拓扑与查询路由算法完全分离。**

建立一个固定的 Knowledge LSDB：

```text
nodes
edges
metadata
```

实验期间 K1–K4 使用完全相同的 LSDB。

Routing 算法不能偷偷改变图。

---

# 11. 第一轮只允许 Hard Edge

不要一开始让 LLM 大量生成：

```text
RELATED_TO
SIMILAR_TO
MAYBE_DEPENDS_ON
```

优先使用可以验证的关系：

```text
PART_OF
REFERENCES
BASED_ON
IMPLEMENTS
DEFINES
AMENDS
SUPERSEDES
VERSION_OF
EXCEPTION_TO
```

以及明确的文档层级关系。

每条边保存：

```text
source
target
relation
provenance
confidence
```

例如：

```json
{
  "source": "doc003#section12",
  "target": "doc001#section7",
  "relation": "REFERENCES",
  "provenance": "explicit_reference",
  "confidence": 1.0
}
```

第一阶段原则：

> 宁可图小，也不要图脏。

Soft Edge 单独留到第二阶段实验。

---

# 12. Route Prefix / Aggregation Node

借 RIFT 思路，将高层公共节点分成：

```text
ROUTE_PREFIX
```

和：

```text
EVIDENCE
```

例如：

```text
医疗机构
中医医院
三级中医医院
```

可以是 Route Prefix。

Route Prefix：

```text
允许：
scope narrowing
hierarchy navigation

禁止：
直接进入生成上下文
无约束展开全部 children
```

因此：

```text
“医疗机构” degree=800
```

本身不是问题。

真正禁止的是：

```text
expand_all("医疗机构")
```

---

# 13. Hub 判定

除了人工定义的 Route Prefix，再提供自动保护。

建议初始规则：

```text
hub_threshold =
max(
    6,
    corpus_nonleaf_degree_p90
)
```

如果：

```text
degree(node) >= hub_threshold
```

则禁止普通：

```text
neighbors(*)
```

只能：

```text
scope_route()
typed_expand()
```

---

# 14. K1：RIFT-style Routing

K1 只实现：

```text
aggregation
scope narrowing
directional expansion
```

基本流程：

```text
Query
 ↓
Vector/FTS Seed
 ↓
定位知识区域
 ↓
Route Prefix narrowing
 ↓
只在目标子域展开
 ↓
Evidence candidates
 ↓
rerank
 ↓
Generator
```

禁止跨整个知识图 BFS。

这是第一个关键 ablation。

如果 K1 已经明显优于 B0/B1，说明过去的重要失败来源确实是：

**Graph flooding。**

RIFT 的原始协议本身就通过限制链路状态信息的传播方向、向下主要使用聚合/default 信息来避免无必要的全拓扑传播。

---

# 15. K2：加入 EIGRP-style Feasibility

K2 不再允许：

```text
所有 neighbor → 排分
```

而采用：

```text
candidate
 ↓
Feasibility Gate
 ↓
ranking
```

候选下一跳首先必须满足：

```text
relation allowed
scope compatible
not visited
not loop
within budget
supports current direction
```

只有通过 Gate 后才计算 ranking。

即：

> 先判断“有没有资格走”，再判断“哪一个最好”。

维护：

```text
successor
feasible_successors
```

例如：

```text
successor = B
backup = [C, D]
```

当前 successor 无效时，优先从 feasible successor 中切换，而不是重新全库检索。

这借鉴的是 EIGRP DUAL 的 Feasibility Condition / Successor / Feasible Successor 思想；RFC 7868 将 feasibility condition 定义为保证无环路径的充分条件。

注意：

**Knowledge Feasibility Condition 是我们自己的类比设计，不声称等价于 EIGRP 的数学保证。**

---

# 16. K3：加入 SRv6-style Segment Plan

Ingress 阶段只读 Question，生成一个短的 Routing Program。

例如：

```text
Question:
当前规定是什么，它的上位依据是什么？

Segment Plan:

RESOLVE_VERSION
FOLLOW_BASIS
CHECK_EXCEPTION
VERIFY
```

定义少量固定 Knowledge SID：

```text
K.RESOLVE_VERSION
K.FOLLOW_REFERENCE
K.FOLLOW_BASIS
K.FIND_DEFINITION
K.CHECK_EXCEPTION
K.CHECK_SCOPE
K.VERIFY
```

不要允许 Agent 任意发明新 operation。

执行时：

```text
Segment 1
→ 找节点

Segment 2
→ 下一跳

Segment 3
→ 下一跳
```

只在 Segment 无法执行时进入 fallback。

SRv6 本身允许 ingress 将一组有序 instructions/segments 编码为路径程序，让沿途节点执行相应行为；我们借的是这种“先形成粗粒度执行程序，再逐段执行”的思想。

---

# 17. K4：Evidence Gap / ACTIVE fallback

K4 是完整系统。

默认：

```text
PASSIVE
```

即存在确定路线时，不让 Agent 自由搜索。

只有：

```text
当前 Segment 无 feasible successor
```

才进入：

```text
ACTIVE
```

Controller 判断：

```text
Evidence sufficient?
```

如果否：

```json
{
  "missing": "当前条款的上位法律依据",
  "relation_family": ["BASED_ON", "REFERENCES"],
  "scope": "..."
}
```

根据 gap 做一次定向 retrieval。

获得证据之后立即重新进入 PASSIVE。

不要让 Agent 无限循环。

S2G-RAG 的核心也是显式判断 evidence sufficiency；不足时生成结构化 gap，再用于下一轮检索，同时维护紧凑 Evidence Context，以减少多轮检索的噪声累积。

---

# 18. Routing Budget

禁止仅设置：

```text
depth=3
```

使用统一 budget：

```yaml
seed_top_k: 5

max_branch_per_node: 2
max_hops: 3
max_paths: 6
max_final_evidence: 6
max_evidence_tokens: 4000

max_active_fallbacks: 2
```

所有 D20/D50/D100 固定。

不能因为 D100 较难就临时调大。

---

# 19. Routing Cost

第一轮不要搞复杂学习模型。

只设置简单 cost：

```text
explicit_reference     1
hierarchy              1
version                 1
definition              1

validated_soft_edge     2

semantic_fallback       3
hub_crossing            4
cross_scope             4
```

Route Controller 优先选择：

```text
低 cost
+
通过 feasibility gate
```

的路径。

不要过早训练 learned router。

---

# 20. Generator Context

最终 Generator 不看：

```text
整个图
全部候选
全部 route prefix
所有搜索结果
```

只看：

```text
Question

Evidence E1
Evidence E2
Evidence E3

Evidence Path / Provenance
```

例如：

```text
E1
doc07 §12

E2
doc03 §4

Path:
E1 --REFERENCES--> E2
```

核心原则：

> Candidate Space ≠ Evidence Context

Graph 可以产生大量候选。

Generator 只能看到经过验证的少量 evidence。

PathRAG 正是针对 GraphRAG 检索冗余问题，将重点从整片邻域转向关键 relational paths。

---

# 21. 主要评价指标

## Primary

```text
Answer Accuracy
```

但不能只看它。

## Retrieval

记录：

```text
Evidence Precision
Evidence Recall
Evidence F1
```

## Context Pollution Rate

定义：

```text
CPR =
distractor evidence count
/
all evidence count
```

越低越好。

## Routing

记录：

```text
route length
candidate count
hub encounters
hub expansions prevented
feasibility rejects
fallback count
successful fallback count
```

## 成本

记录：

```text
retrieval latency
routing latency
LLM latency
end-to-end latency

P50
P95

input tokens
output tokens
total LLM calls
```

---

# 22. 两个特别重要的指标

## Rescue Rate

纯 RAG 错，而 Router 对：

```text
B0 = Wrong
K4 = Correct
```

## Regression Rate

纯 RAG 对，而 Router 错：

```text
B0 = Correct
K4 = Wrong
```

这是必须报告的。

因为旧 GraphRAG 从 40% 跌到 35%，很可能就是：

```text
Regression > Rescue
```

新 Router 真正成功应该是：

```text
Rescue >> Regression
```

---

# 23. 规模退化指标

Core-60 在三个 corpus 都运行。

得到：

```text
                D20    D50    D100

Vector          ?
Routing         ?
```

最值得观察的是：

```text
accuracy_drop =
Accuracy(D20) - Accuracy(D100)
```

如果：

```text
Vector:
50 → 38

Router:
52 → 50
```

那么即使 Router 在 D20 的绝对提升不大，也说明 Routing 成功解决了：

**corpus expansion / distractor pollution。**

---

# 24. Hub Stress Test

从 benchmark 中单独建立一组人工确认的 Hub cases。

记录每道题关键中间节点 degree。

按：

```text
degree < 5
5–10
11–20
21–50
>50
```

分桶。

绘制：

```text
degree
  vs
accuracy
CPR
retrieved_nodes
latency
```

核心目标：

随着 degree 增长：

```text
Vector / Legacy Graph
```

可能下降。

但：

```text
Routing
```

应该更加平稳。

---

# 25. Answer 评判

能够 exact match 的问题自动评分。

无法 exact match 的问题采用：

```text
gold answer
+
gold evidence
+
rubric
```

进行盲评。

评审时不要显示：

```text
B0
K4
Routing
Vector
```

只显示随机：

```text
Output A
Output B
```

LLM Judge 可以作为辅助，但不要作为唯一最终裁判。

对：

```text
baseline/router 不一致
自动 Judge 低置信度
```

的记录进行人工仲裁。

---

# 26. 统计分析

因为每个系统回答完全相同的问题，所以做 paired comparison。

至少报告：

```text
accuracy
absolute delta
relative delta
95% bootstrap CI
```

B0 vs K4 再做：

```text
McNemar exact test
```

同时输出 transition matrix：

```text
B0 correct → K4 correct
B0 correct → K4 wrong
B0 wrong   → K4 correct
B0 wrong   → K4 wrong
```

不要只输出一个总 accuracy。

---

# 27. 成功标准：实验开始前锁定

## Minimum Signal

```text
K4 > B0
```

只能说明值得继续研究。

不能算真正成功。

## Engineering Success

建议提前定义：

```text
Accuracy:
K4 ≥ B0 + 8 percentage points
```

如果 baseline 仍约 40%，也就是目标至少：

```text
≈48%
```

同时：

```text
Regression Rate < Rescue Rate
CPR 显著低于 B0/B1
D20 → D100 退化小于 B0
```

## Strong Success

```text
Accuracy ≥ B0 + 10pp
```

即若 baseline≈40%：

```text
≥50%
```

并且：

```text
P95 ≤ 3.5s
```

如果生成模型本身 API 延迟不可控，则分别要求：

```text
retrieval+routing P95
```

满足预算，并单独报告 LLM latency。

---

# 28. 第一轮实验不做什么

明确禁止 scope creep：

```text
不训练 router
不训练 embedding
不更换 generator
不搞复杂 ontology
不自动生成海量三元组
不引入 Neo4j 作为必要依赖
不优化 UI
不重新做文档解析
不为了分数修改 test questions
```

目标只有：

> 验证 routing mechanism。

---

# 29. 推荐最小实现

第一版完全可以：

```text
Python
SQLite
NetworkX
现有 Vector DB
现有 FTS/BM25
```

Knowledge Graph 不需要先迁到 Neo4j。

节点和边规模只有 100 篇文档时，NetworkX 足够进行这个实验。

---

# 30. 推荐仓库结构

```text
knowledge-routing-exp/
│
├── README.md
├── EXPERIMENT.md
│
├── data/
│   ├── manifests/
│   │   ├── d20.json
│   │   ├── d50.json
│   │   └── d100.json
│   └── README.md
│
├── benchmark/
│   ├── questions.jsonl
│   ├── gold.jsonl
│   └── split.json
│
├── configs/
│   ├── b0_vector.yaml
│   ├── b1_hybrid.yaml
│   ├── b2_legacy_graph.yaml
│   ├── k1_rift.yaml
│   ├── k2_feasibility.yaml
│   ├── k3_segments.yaml
│   └── k4_full.yaml
│
├── src/
│   ├── index/
│   ├── graph/
│   ├── retrieval/
│   ├── routing/
│   └── evaluation/
│
├── runs/
│   └── .gitkeep
│
└── reports/
    ├── results.csv
    ├── transitions.csv
    ├── hub_analysis.csv
    └── final_report.md
```

如果原始文档有版权、内部数据或隐私：

**GitHub 只提交 manifest/hash，不提交原文。**

---

# 31. 每次 Run 必须保存完整 Trace

每个 question 输出一条 JSONL：

```json
{
  "qid": "Q0042",
  "system": "K4",
  "corpus": "D100",

  "seeds": [],
  "segment_plan": [],
  "visited_nodes": [],
  "rejected_nodes": [],
  "selected_paths": [],
  "final_evidence": [],

  "answer": "",
  "correct": null,

  "retrieval_ms": 0,
  "routing_ms": 0,
  "generation_ms": 0,

  "input_tokens": 0,
  "output_tokens": 0
}
```

这样出现错误时可以回答：

> 到底在哪一跳走错？

而不是只留下 final answer。

---

# 32. 执行顺序

不要一口气写完整 K4。

严格按照：

```text
Phase 0
建立 benchmark + gold

Phase 1
B0 / B1 baseline

Phase 2
如果可能，复现 B2 Legacy Graph

Phase 3
实现 K1
运行 Dev

Phase 4
实现 K2
运行 Dev

Phase 5
实现 K3
运行 Dev

Phase 6
实现 K4
运行 Dev

──── 参数冻结 ────

Phase 7
正式运行 Test：
D20
D50
D100

Phase 8
自动统计

Phase 9
blind adjudication

Phase 10
生成 final_report.md
```

禁止：

```text
看 Test
→ 修改 Router
→ 再跑 Test
```

如果需要修改算法，建立：

```text
Experiment V2
```

重新记录。

---

# 33. 最终报告必须回答的问题

最后不要写成“我们实现了很多功能”。

只回答：

### 1.

纯 RAG baseline 是多少？

### 2.

完整 Routing-RAG 是多少？

### 3.

提升主要来自哪一个 ablation？

```text
K1?
K2?
K3?
K4?
```

### 4.

从 D20 → D100 后谁退化最快？

### 5.

Hub degree 增加后谁退化最快？

### 6.

Routing 修复了多少：

```text
Vector wrong → Router correct
```

又破坏了多少：

```text
Vector correct → Router wrong
```

### 7.

错误仍主要来自：

```text
seed retrieval
scope selection
wrong next-hop
missing edge
wrong stopping
generator misuse
```

哪一种？

### 8.

准确率收益是否值得 latency/token 成本？

---

# 34. Go / No-Go

实验结束以后只做三个结论之一。

## GO

例如：

```text
B0 = 40%
K4 = 51%

CPR ↓
D100 degradation ↓
P95 acceptable
```

继续建设 Knowledge Routing。

## ITERATE

例如：

```text
B0 = 40%
K4 = 45%

Evidence Precision 明显提升
但 route recall 不足
```

说明方向有信号，但需要改 Router。

## NO-GO

例如：

```text
B0 = 40%
K4 = 40~42%

复杂度 ↑
Latency ↑
Evidence quality 没明显改善
```

停止继续堆路由机制。

这是一个完全可以接受的实验结论。

---

# 35. 本实验真正要证明的命题

最后不要写：

> Graph 比 Vector 强。

而应该验证：

> **在存在跨文档依赖、高扇出公共节点和多跳证据链的知识库中，将“候选发现”和“证据进入上下文”解耦，并通过聚合、可行性约束和显式路径程序控制下一跳，是否能提高证据精度和最终回答准确率。**

如果答案是否定的，就停止。

如果答案是肯定的，再考虑把它做成完整项目。