# Knowledge Routing RAG 实验准备清单

用途：

> 在正式执行实验方案前，确认语料、Embedding、DeepSeek、图构建条件、评测材料和运行环境已经准备完成。

原则：

- 本清单只负责准备与确认。
- 不修改既定实验方案。
- 未确定项可以留空。
- agy 不应自行替用户修改核心实验条件。
- 正式 Test 开始后，不再修改已冻结的实验参数。

---

## 0. 基本信息

### 实验仓库

```text
GitHub Repo:
________________________________________
```

### 本地项目目录

```text
Project Path:
________________________________________
```

### 实验机器 / 环境

```text
OS:
________________________________________

CPU:
________________________________________

RAM:
________________________________________

GPU（如使用）:
________________________________________

Python Version:
________________________________________
```

### 是否使用 Docker

```text
[ ] 是
[ ] 否
```

如是：

```text
Docker Version:
________________________________________

Docker Compose Version:
________________________________________
```

---

# 1. 文档语料

## 1.1 文档总数

目标：

```text
100 个已完成预处理的结构化 TXT / MD 文档
```

完成情况：

```text
[ ] 已准备 100 个
[ ] 未完成
```

实际数量：

```text
________________________________________
```

---

## 1.2 文档目录

```text
Corpus Path:
________________________________________
```

例如：

```text
data/documents/
```

---

## 1.3 文档格式

允许：

```text
[ ] Markdown (.md)
[ ] TXT (.txt)
[ ] 两者混合
```

其他：

```text
________________________________________
```

---

## 1.4 文档是否已完成文本预处理

确认：

```text
[ ] 不需要 OCR
[ ] 不需要 PDF 解析
[ ] 不需要网页正文提取
[ ] 无明显乱码
[ ] 标题与正文基本可识别
[ ] 文本编码统一
```

默认推荐：

```text
UTF-8
```

实际编码：

```text
________________________________________
```

---

# 2. D20 / D50 / D100 语料集合

必须满足：

```text
D20 ⊂ D50 ⊂ D100
```

即：

```text
D20  = 前 20 个固定文档
D50  = D20 + 30 个文档
D100 = D50 + 50 个文档
```

确认：

```text
[ ] 可以构造严格嵌套的 D20 / D50 / D100
[ ] 不可以
```

文档 ID 命名方式：

```text
________________________________________
```

推荐：

```text
doc001
doc002
...
doc100
```

---

## 2.1 D20 文档清单

路径或 manifest：

```text
________________________________________
```

---

## 2.2 D50 文档清单

路径或 manifest：

```text
________________________________________
```

---

## 2.3 D100 文档清单

路径或 manifest：

```text
________________________________________
```

---

# 3. 文档结构情况

用于判断 Navigation Graph 可以从哪些确定性结构生成。

## 3.1 文档是否有明确标题层级

例如：

```text
# 一级标题
## 二级标题
### 三级标题
```

或：

```text
第一章
第一节
第一条
```

确认：

```text
[ ] 大多数文档存在稳定层级
[ ] 部分存在
[ ] 基本不存在
```

说明：

```text
________________________________________
```

---

## 3.2 是否存在显式跨文档引用

例如：

```text
根据《XXXX办法》
依据《XXXX条例》第十二条
参照《XXXX标准》
按照前款规定
见附件 X
```

确认：

```text
[ ] 大量存在
[ ] 少量存在
[ ] 基本不存在
[ ] 尚未检查
```

示例：

```text
________________________________________

________________________________________
```

---

## 3.3 是否存在版本 / 修订关系

例如：

```text
废止
修订
替代
修正
自某日起施行
原文件同时废止
```

确认：

```text
[ ] 大量存在
[ ] 少量存在
[ ] 基本不存在
[ ] 尚未检查
```

---

## 3.4 是否存在例外 / 特殊适用条件

例如：

```text
但……
除……外
特殊情况下……
本规定不适用于……
```

确认：

```text
[ ] 存在
[ ] 基本不存在
[ ] 尚未检查
```

---

# 4. 文档 Metadata

以下字段如果已有，请填写。

```text
document_id:
________________________________________

title:
________________________________________

document_type:
________________________________________

issuer / source:
________________________________________

publish_date:
________________________________________

effective_date:
________________________________________

expiry_date:
________________________________________

version:
________________________________________

status:
________________________________________

jurisdiction:
________________________________________

applicable_scope:
________________________________________
```

Metadata 当前状态：

```text
[ ] 已结构化保存
[ ] 文本中存在，但未结构化
[ ] 部分存在
[ ] 基本没有
```

Metadata 文件路径：

```text
________________________________________
```

---

# 5. Chunking

实验中所有系统必须共用相同 chunk。

当前状态：

```text
[ ] 已经完成 chunking
[ ] 尚未 chunk
```

如果已经完成：

```text
Chunk 文件路径:
________________________________________

Chunk 平均长度:
________________________________________

Chunk overlap:
________________________________________

Chunk 数量:
________________________________________
```

如果尚未确定，由实验代码统一生成，但必须在 B0–K4 间完全一致。

建议保留：

```text
doc_id
section_id
chunk_id
heading_path
order
text
```

确认：

```text
[ ] 可以保留上述字段
[ ] 无法全部保留
```

---

# 6. Embedding 服务

实验方案要求：

> 所有系统使用同一个 Embedding 服务、同一套向量。

当前状态：

```text
[ ] Docker Embedding API 已部署
[ ] 尚未完成
```

### API Endpoint

```text
________________________________________
```

### Model

```text
________________________________________
```

### Embedding Dimension

```text
________________________________________
```

### API 协议

例如：

```text
OpenAI-compatible
自定义 HTTP API
```

实际：

```text
________________________________________
```

### 输入字段 / API 示例

```text
________________________________________
```

### 是否需要认证

```text
[ ] 不需要
[ ] API Key
[ ] 其他
```

环境变量名：

```text
________________________________________
```

### Similarity

```text
[ ] cosine
[ ] dot product
[ ] euclidean
[ ] 尚未确认
```

### Embedding 是否归一化

```text
[ ] 是
[ ] 否
[ ] 尚未确认
```

---

# 7. Vector Index

计划使用的向量数据库 / 索引：

```text
________________________________________
```

例如：

```text
FAISS
Qdrant
Milvus
Chroma
pgvector
```

当前状态：

```text
[ ] 已部署
[ ] 尚未部署
[ ] 尚未决定
```

要求：

```text
[ ] 支持 doc_id filter
```

最好实现：

```text
D20  = filter doc_id ∈ D20
D50  = filter doc_id ∈ D50
D100 = 全量
```

这样无需为三个规模分别重新 embedding。

---

# 8. FTS / BM25

B1 以及 Routing 系统需要 FTS/BM25。

计划使用：

```text
________________________________________
```

例如：

```text
SQLite FTS5
BM25 library
Elasticsearch
OpenSearch
PostgreSQL FTS
```

状态：

```text
[ ] 已有
[ ] 尚未实现
[ ] 由 agy 实现
```

中文分词方案：

```text
________________________________________
```

如暂未确定可留空。

---

# 9. Navigation Graph

实验方案保持不变：

> 第一轮只建立 Hard Navigation Graph，不建立大而全的自动知识图谱。

---

## 9.1 图节点

计划支持：

```text
[ ] DOCUMENT
[ ] SECTION
[ ] CHUNK
[ ] ROUTE_PREFIX
```

其他：

```text
________________________________________
```

---

## 9.2 结构关系

允许确定性生成：

```text
[ ] PART_OF
[ ] HAS_CHILD
[ ] NEXT
[ ] PREVIOUS
```

---

## 9.3 显式知识关系

根据真实语料选择：

```text
[ ] REFERENCES
[ ] BASED_ON
[ ] IMPLEMENTS
[ ] DEFINES
[ ] AMENDS
[ ] SUPERSEDES
[ ] VERSION_OF
[ ] EXCEPTION_TO
```

其他：

```text
________________________________________
```

---

## 9.4 图生成方式

第一轮固定为：

```text
[✓] 文档结构解析
[✓] 显式引用解析
[✓] Metadata 关系
[✓] 必要的人工校验

[✗] LLM 大规模自动三元组生成
[✗] 无依据 RELATED_TO
[✗] 无依据 SIMILAR_TO
```

---

## 9.5 Graph Storage

默认建议：

```text
NetworkX
+
JSONL / SQLite
```

实际计划：

```text
________________________________________
```

第一轮：

```text
[ ] 不使用 Neo4j
[ ] 确有必要使用 Neo4j
```

---

# 10. DeepSeek API

实验统一使用：

```text
DeepSeek V4.1 Flash
```

API 当前状态：

```text
[ ] API Key 已准备
[ ] 尚未准备
```

### Endpoint

如果官方：

```text
https://api.deepseek.com
```

实际：

```text
________________________________________
```

### Model ID

当前计划：

```text
deepseek-flash
```

实际：

```text
________________________________________
```

### API Key 环境变量

推荐：

```text
DEEPSEEK_API_KEY
```

实际：

```text
________________________________________
```

---

# 11. DeepSeek 在实验中的允许用途

固定如下。

## 所有实验组

DeepSeek 用于：

```text
[✓] Final Answer Generation
```

不得用于：

```text
[✗] Embedding
[✗] B0/B1 retrieval
[✗] 自动构建 Hard Graph
[✗] LLM reranking
```

---

## K3

增加：

```text
[✓] Segment Planner
```

---

## K4

增加：

```text
[✓] Segment Planner
[✓] Evidence Gap / ACTIVE fallback
```

除此之外不得增加新的 LLM 决策环节。

---

# 12. Final Answer 输出格式

所有 B0–K4 使用统一格式。

建议：

```json
{
  "answer": "...",
  "evidence_ids": ["E1", "E3"],
  "insufficient_evidence": false
}
```

确认：

```text
[ ] 使用该格式
[ ] 需要调整
```

调整内容：

```text
________________________________________
```

---

# 13. Knowledge Segment Vocabulary

K3/K4 只能从预定义操作中选择。

初始集合：

```text
RESOLVE_VERSION
FOLLOW_REFERENCE
FOLLOW_BASIS
FIND_DEFINITION
FIND_IMPLEMENTATION
CHECK_EXCEPTION
CHECK_SCOPE
CHECK_AUTHORITY
VERIFY
```

需要增加：

```text
________________________________________

________________________________________
```

需要删除：

```text
________________________________________
```

---

# 14. Benchmark

目标建议：

```text
120 questions
```

当前已有问题数量：

```text
________________________________________
```

状态：

```text
[ ] 已有现成问题集
[ ] 可以从旧 113 条测试中抽取
[ ] 需要重新制作
```

注意：

> 旧 113 条如果已经参与过系统分析，应优先作为 Dev / regression 数据，不宜全部当 blind Test。

---

## 14.1 Gold Answer

当前状态：

```text
[ ] 已有
[ ] 部分已有
[ ] 尚未制作
```

---

## 14.2 Gold Evidence

每题应至少知道：

```text
gold_documents
gold_spans
```

理想情况下：

```text
gold_path
```

当前状态：

```text
[ ] 已有
[ ] 部分已有
[ ] 尚未制作
```

---

## 14.3 题型标签

准备支持：

```text
[ ] single-hop
[ ] 2-hop
[ ] 3-hop+
[ ] hub/high-fanout
[ ] temporal/version
[ ] exception/conflict
```

---

# 15. Core-60

必须准备一批：

```text
答案证据全部位于 D20
```

的问题。

目标：

```text
60 questions
```

当前数量：

```text
________________________________________
```

它们将在：

```text
D20
D50
D100
```

重复运行。

---

# 16. Dev / Test Split

目标：

```text
Dev  = 24
Test = 96
```

或保持相近比例：

```text
20% Dev
80% Test
```

确认：

```text
[ ] 接受
[ ] 需要调整
```

调整：

```text
________________________________________
```

Test 固定后：

```text
[ ] 不根据 Test 调参数
```

---

# 17. Baseline

实验 baseline 必须重新运行。

不能直接使用历史：

```text
Vector-only ≈ 40%
```

作为正式 baseline。

需要运行：

```text
B0 = Vector-only
B1 = Vector + FTS
```

如果旧实现可用：

```text
B2 = Legacy Vector + FTS + Graph
```

旧 B2 代码位置：

```text
________________________________________
```

如果没有：

```text
[ ] B2 标记为 optional
```

---

# 18. 初始 Retrieval 参数

第一轮建议：

```yaml
vector_top_k: 5
fts_top_k: 5
final_seed_k: 5
```

Hybrid 权重：

```yaml
vector_weight: ______
fts_weight: ______
```

如未决定，可在 Dev 阶段确定。

---

# 19. 初始 Routing Budget

建议起点：

```yaml
max_hops: 3
max_branch_per_node: 2
max_paths: 6
max_final_evidence: 6
max_active_fallbacks: 2
max_evidence_tokens: 4000
```

确认：

```text
[ ] 使用默认值作为 Dev 起点
[ ] 需要调整
```

调整：

```text
________________________________________
```

---

# 20. Hub / Route Prefix

需要确认是否存在高连接度概念。

已知或疑似 Hub：

```text
1. ____________________________________

2. ____________________________________

3. ____________________________________

4. ____________________________________
```

例如：

```text
医疗机构
卫生行政部门
人员
管理
```

自动 Hub Threshold：

```text
[ ] 由 degree distribution 在 Dev 阶段计算
```

建议起点：

```text
max(6, degree P90)
```

---

# 21. 评测

自动评分可支持：

```text
[ ] Exact Match
[ ] Numeric Match
[ ] Entity Match
[ ] Evidence Recall
[ ] Evidence Precision
```

需要人工/模型仲裁的题：

```text
________________________________________
```

---

## 21.1 人工仲裁

仲裁人员：

```text
________________________________________
```

如果只有自己：

```text
[ ] 接受单人 blind adjudication
```

要求：

```text
不显示 B0/K4 等系统标签
```

---

# 22. 实验指标

确认记录：

```text
[✓] Answer Accuracy

[✓] Evidence Precision
[✓] Evidence Recall
[✓] Evidence F1

[✓] Context Pollution Rate

[✓] Rescue Rate
[✓] Regression Rate

[✓] route length
[✓] candidate count
[✓] hub encounters
[✓] feasibility rejects
[✓] fallback count

[✓] P50 latency
[✓] P95 latency

[✓] input tokens
[✓] output tokens
[✓] LLM calls
```

---

# 23. Trace 保存

每道题都需要保存完整执行轨迹：

```text
qid
system
corpus

seeds
segment_plan
visited_nodes
rejected_nodes
selected_paths
final_evidence

answer
score

retrieval_ms
routing_ms
generation_ms

input_tokens
output_tokens
```

Trace Path：

```text
________________________________________
```

推荐：

```text
runs/<run_id>/traces.jsonl
```

---

# 24. GitHub 安全检查

必须：

```text
[ ] .env 加入 .gitignore
[ ] API Key 不进入 Git
[ ] HTTP Authorization Header 不写 trace
[ ] 数据中无不应公开的隐私信息
[ ] 原始语料版权允许上传
```

如果原始语料不能上传：

```text
[ ] GitHub 只提交 manifest / hash / 示例数据
```

---

# 25. 环境变量模板

计划：

```bash
DEEPSEEK_API_KEY=

EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=

VECTOR_DB_URL=
VECTOR_DB_API_KEY=
```

实际还需要：

```text
________________________________________

________________________________________
```

---

# 26. 正式实验前 Smoke Test

必须全部通过。

## Embedding

```text
[ ] API 可访问
[ ] 输入文本可正常生成向量
[ ] 向量维度固定
[ ] 同一接口能批量处理文本
```

## Vector Search

```text
[ ] 可以写入
[ ] 可以查询
[ ] 可以按 doc_id filter
```

## FTS

```text
[ ] 可以索引
[ ] 可以检索
```

## DeepSeek

```text
[ ] API 可访问
[ ] deepseek-flash 可正常调用
[ ] 结构化输出可正常解析
```

## Corpus

```text
[ ] D20 ⊂ D50 ⊂ D100
[ ] 100 文档均可读取
[ ] 文档 ID 无重复
```

## Graph

```text
[ ] 能生成 Document → Section → Chunk 结构
[ ] 显式引用解析可运行
[ ] edge provenance 可追踪
```

---

# 27. Phase -1 输出要求

在正式实现 Routing 之前，agy 应先生成：

```text
preflight_report.md
corpus_manifest.json
relation_statistics.json
```

其中 `preflight_report.md` 至少回答：

```text
1. 实际读取多少文档？
2. D20/D50/D100 是否严格嵌套？
3. 总 chunk 数是多少？
4. Embedding 服务是否正常？
5. DeepSeek API 是否正常？
6. 文档结构关系能生成多少？
7. 显式跨文档引用能识别多少？
8. 版本/废止/修订关系能识别多少？
9. 图节点数、边数是多少？
10. degree 分布如何？
11. 哪些节点属于高 fan-out Hub？
12. 哪些关系无法确定性抽取？
```

完成后：

```text
STOP
```

不要立刻开始修改实验设计。

只有确认 preflight 没有严重问题之后，再继续执行既定实验方案。

---

# 28. 最终交给 agy 的材料

正式开始前，应提供：

```text
[ ] 01_EXPERIMENT_PLAN.md
    完整实验方案

[ ] 02_PREPARATION_CHECKLIST.md
    本准备清单

[ ] Corpus
    20/50/100 所需文档

[ ] Embedding API 配置

[ ] DeepSeek API 环境变量

[ ] Benchmark / 已有测试题

[ ] Gold answer / evidence（已有部分）

[ ] 旧实验报告或结果（如有）

[ ] 旧 GraphRAG 实现（如需要复现 B2）
```

---

# 29. 当前待填写项摘要

真正需要用户准备/填写的主要是：

```text
Embedding API Endpoint:
________________________________________

Embedding Model:
________________________________________

Embedding Dimension:
________________________________________

Vector DB:
________________________________________

100 文档目录:
________________________________________

文档 Metadata 情况:
________________________________________

显式引用情况:
________________________________________

DeepSeek API Endpoint:
________________________________________

DeepSeek API Key 环境变量:
________________________________________

现有 Benchmark:
________________________________________

现有 Gold Evidence:
________________________________________

旧 GraphRAG 代码/报告:
________________________________________

GitHub Repo:
________________________________________
```

其余没有填写的技术细节，由 agy 按既定实验方案的默认值实现，不得自行改变实验目标。